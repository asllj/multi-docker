sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t test/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true test/react-test npm test

after_success:
  - docker build -t test/multi-client ./client
  - docker build -t test/multi-nginx ./nginx
  - docker build -t test/multi-server ./server
  - docker build -t test/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push test/multi-client
  - docker push test/multi-nginx
  - docker push test/multi-server
  - docker push test/multi-worker
